---
allowed-tools: Read(*), Write(*)
description: 將英文技術文檔整理成繁體中文精簡列點格式
---

請將英文技術文檔內容整理成繁體中文的精簡列點格式。

**使用者會提供：**

1. 檔案路徑
2. 要整理的章節名稱
3. 待整理的英文內容

請先讀取指定檔案，找到對應章節，然後將整理後的內容直接更新到該章節位置。

**使用者提供的資訊：** $ARGUMENTS

## 整理規則

### 語言與標點

- 使用繁體中文
- 筆記整理全程使用**中文標點符號**，只有程式碼內保持使用英文標點符號

  - **請嚴格遵守此規則**以確保文件一致性，參考下表：

    | 中文標點 | 中文碼位 | 英文標點 | 英文碼位 |
    | -------- | -------- | -------- | -------- |
    | （       | 0xFF08   | (        | 0x0028   |
    | ）       | 0xFF09   | )        | 0x0029   |
    | ，       | 0xFF0C   | ,        | 0x002C   |
    | 。       | 0x3002   | .        | 0x002E   |
    | ：       | 0xFF1A   | :        | 0x003A   |
    | ；       | 0xFF1B   | ;        | 0x003B   |

- 技術術語附上英文原文，格式：中文（English）

### 格式要求

- 使用列點（-）結構組織內容
- 重要概念使用 **粗體** 強調
- 適當使用子列點表達層級關係
- 保留程式碼範例，並在前後加上簡短說明
- 根據內容需要使用適當層級的子標題（###、#### 等）
- 層級最多用到第四層標題（####）
- 不需要使用分隔線（---）

### 內容處理

- 精簡扼要，去除冗餘描述
- 保留核心概念和關鍵資訊
- 必要時加入對比說明或實際範例
- 重要提醒使用 `> 💡` 引用區塊或 `⚠️` 符號標示

## 範例

**指令格式：**

```bash
/note-zh 檔案路徑 章節名稱 待整理的英文內容
```

**實際範例：**

```bash
/note-zh Coroutines.md "The Framework" Coroutines are stackless. A stackless coroutine is able to suspend and resume the top-level coroutine. Stackless coroutines are often called resumable functions.
```

執行後會：

1. 讀取 Coroutines.md
2. 找到 "The Framework" 章節
3. 將英文內容整理成繁體中文列點格式
4. 更新到該章節位置

**整理後的輸出：**

```markdown
- **無堆疊（Stackless）**
  - 能暫停和恢復頂層協程
  - 又稱可恢復函數（resumable functions）
```
